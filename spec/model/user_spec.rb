# this is a mock test file to validate the setup and Travis hooks; its contents
# will be replaced

require 'rails_helper'

describe User do
  it "does something userful" do
    expect(3).to eq(3)
  end
end